# Fertilizer Prediction System — Python Dependencies
# Install with: pip install -r requirements.txt

# Core ML & data
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
openpyxl>=3.1.0        # Excel (.xlsx) reading for train_model.py
joblib>=1.3.0           # Model serialisation (bundled with scikit-learn)

# Web API
flask>=3.0.0
gunicorn>=21.0.0   # Production WSGI server for deployment

# Optional — for Jupyter notebook exploration
# jupyter>=1.0.0
# matplotlib>=3.7.0
# seaborn>=0.12.0
